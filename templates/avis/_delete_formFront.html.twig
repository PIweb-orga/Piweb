<form method="post" action="{{ path('app_avis_deleteFront', {'id': avi.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avi.id) }}">
     <button class="btn btn-danger rounded-pill py-2 px-4"
><i class="fa fa-trash fa-spin"></i>
</button>
</form>
