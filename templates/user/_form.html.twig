{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
<div class="fils" style="padding-left: 300px;">
    <div class="row">
        <div class="col-md-6">
           

            <!-- Champ username -->
            <div class="mb-3">
                {{ form_label(form.username, 'username', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.username, {'attr': {'class': 'form-control' ~ (form.username.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Username'}}) }}
                {% if form.username.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.username)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>
             <!-- Champ Email -->
            <div class="mb-3">
                {{ form_label(form.email, 'Email', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.email, {'attr': {'class': 'form-control' ~ (form.email.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Email'}}) }}
                {% if form.email.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.email)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>
 <!-- Champ password -->
            <div class="mb-3">
                {{ form_label(form.password, 'Password', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.password, {'attr': {'class': 'form-control' ~ (form.password.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Password'}}) }}
                {% if form.password.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.password)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>
 <!-- Champ firstname  -->
            <div class="mb-3">
                {{ form_label(form.firstname, 'Firstname', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.firstname, {'attr': {'class': 'form-control' ~ (form.firstname.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Firstname'}}) }}
                {% if form.firstname.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.firstname)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>
 <!-- Champ lastname -->
            <div class="mb-3">
                {{ form_label(form.lastname, 'lastname', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.lastname, {'attr': {'class': 'form-control' ~ (form.lastname.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Lastname'}}) }}
                {% if form.lastname.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.lastname)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>
 <!-- Champ tel -->
            <div class="mb-3">
                {{ form_label(form.tel, 'Tel', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.tel, {'attr': {'class': 'form-control' ~ (form.tel.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Tel'}}) }}
                {% if form.tel.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.tel)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>

            <!-- Champ address -->
            <div class="mb-3">
                {{ form_label(form.address, 'Address', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.address, {'attr': {'class': 'form-control' ~ (form.address.vars.errors|length > 0 ? ' is-invalid' : ''), 'placeholder': 'Address'}}) }}
                {% if form.address.vars.errors|length > 0 %}
                    <div class="invalid-feedback">
                        {{ form_errors(form.address)|join(', ')|raw }}
                    </div>
                {% endif %}
            </div>

        </div>
    </div>
{{ form_row(form.recaptcha) }} 
<span class="error-message">{{ form_errors(form.recaptcha) }}</span>
    <div class="mt-3">
        <button type="submit" class="btn btn-primary">Save</button>
    </div>
</div>
{{ form_end(form) }}