{% extends 'front.html.twig' %}

{% block title %}Badge index{% endblock %}

{% block bodyr %}
</br>

    <h1> Tu peux ajouter un Badge </h1>
</br>
    <table class="table">
        <thead>
            <tr>
                <th>Restaurant</th>
                <th>Commantaire</th>
                <th>Datebadge</th>
                <th>Typebadge</th>
            </tr>
        </thead>
        <tbody>
        {% for badge in badges %}
            <tr>
                <td>{{ badge.restaurant.nom }}</td>
                <td>{{ badge.commantaire }}</td>
                <td>{{ badge.datebadge ? badge.datebadge|date('Y-m-d') : '' }}</td>
                <td>{{ badge.typebadge }}</td>
                <td>
                   <a href="{{ path('app_badge_show66', {'id': badge.id}) }}" class="btn btn-success rounded-pill py-2 px-4">
									<i class="fas fa-eye fa-spin"></i>
                                    </a>
</td>
  <td>
        <a href="{{ path('app_badge_like', {'id': badge.id}) }}" class="btn btn-success rounded-pill py-2 px-4">
            <i class="fas fa-thumbs-up"></i> Like {{ badge.likes }}
        </a>
    </td>

    <td>
        <a href="{{ path('app_badge_dislike', {'id': badge.id}) }}" class="btn btn-danger rounded-pill py-2 px-4">
            <i class="fas fa-thumbs-down"></i> Dislike {{ badge.dislikes }}
        </a>
    </td>
<td>
               
      <a href="{{ path('app_badge_editFront', {'id': badge.id}) }}" class="btn btn-success rounded-pill py-2 px-4">
									<i class="fas fa-edit fa-spin"></i></a>
</td>
                </td>
                <td> {{ include('badge/_delete_formFront.html.twig') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
                   <a href="{{ path('app_badge_new66') }}" class="btn btn-success rounded-pill py-2 px-4">
<i > create</i>
                                    </a>
   {# <form method="get" action="{{ path('app_badge_new66') }}">
    <button type="submit" class="btn btn-primary">Create new</button>
</form> #}

    </div>
    <style>
     .button-container {
    display: flex;
    flex-direction: row;
    overflow-x: auto; 
    white-space: nowrap;
}


.button-container button {
    margin-right: 10px; /* Espacement entre les boutons */
}</style>
{% endblock %}

