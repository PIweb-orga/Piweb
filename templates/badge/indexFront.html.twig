{% extends 'front.html.twig' %}

{% block title %}Badge index{% endblock %}

{% block bodyr %}
</br>

    <h1> Tu peux ajouter un Badge </h1>
</br>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Commantaire</th>
                <th>Datebadge</th>
                <th>Typebadge</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for badge in badges %}
            <tr>
                <td>{{ badge.id }}</td>
                <td>{{ badge.commantaire }}</td>
                <td>{{ badge.datebadge ? badge.datebadge|date('Y-m-d') : '' }}</td>
                <td>{{ badge.typebadge }}</td>
                <td>
                                    <button class="btn btn-secondary" onclick="window.location.href='{{ path('app_badge_show66', {'id': badge.id}) }}'">
    Show
</button>
</td>
<td>
                  <button class="btn btn-warning" onclick="window.location.href='{{ path('app_badge_editFront', {'id': badge.id}) }}'">
    Edit
</button>
                </td>
                <td> {{ include('badge/_delete_formFront.html.twig') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

   <form method="get" action="{{ path('app_badge_new66') }}">
    <button type="submit" class="btn btn-primary">Create new</button>
</form>

    </div>
{% endblock %}
