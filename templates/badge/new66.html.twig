{% extends 'front.html.twig' %}

{% block title %}New Badge{% endblock %}

{% block bodyr %}
    <h1>Create new Badge</h1>

   <section id="nouvelle-badge">
        {{ include('badge/_form.html.twig') }}
    </section>
        <div class="fils" style="padding-left: 300px;">
    <form method="get" action="{{ path('app_badge_indexFront') }}">
    <button type="submit" class="btn btn-primary">back to list</button>
</form>
</div>
   
{% endblock %}
