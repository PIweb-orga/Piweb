    <form method="post" action="{{ path('app_badge_deleteFront', {'id': badge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ badge.id) }}">
        <button type="submit" class="btn btn-danger">
            <i class="fa fa-trash"></i> 
        </button>
